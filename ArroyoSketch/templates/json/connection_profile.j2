{
    "name": "{{ profile_name }}",
    "connector": "kafka",
    "config": {
        "authentication": {},
        "bootstrapServers": "{{ bootstrap_servers }}",
        "name": "{{ profile_name }}",
        "schemaRegistryEnum": {}
    }
}
